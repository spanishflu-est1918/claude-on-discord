# claude-on-discord

Personal Claude Code bridge for Discord, powered by Bun + TypeScript.

## Setup

Install dependencies:

```bash
bun install
```

Copy env file:

```bash
cp .env.example .env
```

Run tests:

```bash
bun test
```

Run dev mode:

```bash
bun run dev
```
